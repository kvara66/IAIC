# 작업 전 체크리스트

**새로 수정하거나 학습을 돌리기 전에 매번 이 순서로 확인합니다.**

2026-08-06, 규칙을 확인하지 않아 며칠치 작업이 무효가 된 뒤 만들었습니다.

---

## 0. 규칙 원문을 다시 연다 — 이게 항상 첫 번째

**https://dacon.io/competitions/official/236736/overview/rules**

기억에 의존하지 마세요. **대회 중간에 규칙이 바뀝니다.**

실제로 8월 3일 11시에 "Submission Kit 사용 범위" 조항이 추가됐는데, 저희는 같은 시점에 바뀐 제출 횟수만 보고 규칙 페이지를 다시 안 읽었습니다. 그날 밤부터 위반이 시작됐습니다.

**확인할 것**
- 조항이 추가·변경됐나? (제목에 날짜가 붙은 항목을 특히 볼 것)
- **지금 하려는 이 작업에 걸리는 조항이 있나?**
- 문의에 대한 답변이 와서 해석이 바뀐 게 있나?

---

## 1. submission_kit이 이 작업에 관여하나

**가장 중요한 질문입니다.**

- 킷을 실행하나?
- 킷이 뱉은 값(Action 점수 등)을 보고 무언가 정하나?
- 킷에서 나온 정보가 학습·추론·선택·보정에 흘러들어가나?

**하나라도 예 → 중단.**

> Submission Kit에 포함된 코드, 평가용 모델, checkpoint, 가중치 **및 이를 통해 추출한 정보는** 모델의 학습, 추론, 영상 선택·보정·수정 등 최종 결과 생성 과정에 활용할 수 없습니다.

킷은 **최종 mp4가 확정된 뒤 제출 CSV를 만들 때 딱 한 번만** 돌립니다.

**판별법** — "채점기 없이도 이 판단이 서나?"를 물어보세요. 채점기가 필요하다고 느껴지는 순간이 멈춰야 할 지점입니다.

---

## 2. 이 판단이 샘플마다 다른가

- **216개에 똑같이 적용되는 설정 하나를 고르는 것** → ✅ 하이퍼파라미터 튜닝, 정상
- **샘플마다 다른 답을 고르는 것** → 🚫 test에서 뽑은 정보를 쓰고 있다는 신호

> 특히 **Test 데이터에서 추출한 정보를 활용하여 예측 결과를 보정하거나 선택하는 방식**은 평가 데이터 정보 활용으로 판단될 수 있습니다.

킷을 안 쓰더라도 이 문장에 걸릴 수 있습니다. 자체 지표로 샘플별 선택·보정을 하려면 **먼저 문의하세요.**

---

## 3. 추론이 1시간 안에 들어가나

> GPU: RTX PRO 6000 (96GB VRAM) 1대 기준, **추론 시간 제한: 평가 데이터(eval)에 대해 1시간 이내**

실측 기준 — A40에서 216개 1회 생성이 **100분**. RTX PRO 6000이면 30~50분으로 예산 안입니다. **여러 번 돌리는 설계는 이 시점에서 걸러집니다.**

학습은 **최대 4일**이라 여유롭습니다(우리는 8~14시간).

---

## 4. eval 데이터가 학습에 닿나

> 제공하는 평가 데이터(eval)는 **어떠한 형태로도 모델 학습에 활용할 수 없습니다.**
> 모델 학습에서 평가 데이터 정보 활용(Data Leakage)시 **수상 제외** (Pseudo Labeling 포함)

**제재가 가장 무거운 항목입니다.** 학습 설정의 `root`가 `data/train`을 가리키는지 매번 확인하세요.

---

## 5. 제출 코드가 이걸 재현할 수 있나

> 제출 코드는 Private Score **완전 복원 필수**
> 사용 가능 언어: **Python** / 파일: `.py` 또는 `.ipynb`

- 시드가 고정돼 있나?
- **`.sh` 스크립트로 짜고 있지 않나?** 최종 제출은 Python이어야 합니다
- 1시간 안에 이 코드가 도는가?

---

## 6. 사전학습 모델을 새로 쓴다면

> 공식적으로 가중치가 공개된 사전학습모델 중, 상업적 또는 비상업적 이용이 허용된 라이선스(MIT, Apache 2.0, CC BY, CC BY-NC 등)만 사용 가능
> 원격 API로만 접근 가능한 모델(OpenAI, Gemini 등) 불가

라이선스를 **먼저** 확인하고, 추론 시간도 재보세요. 백본이 커지면 3번에서 걸립니다.

---

## 그 외 기억할 것

| | |
|---|---|
| 팀 구성 마감 | **8/17 23:59** (2~5인) |
| 코드 제출 | **8/21 14:00**, `aix-server@inha.ac.kr` |
| 일일 제출 | 10회 (8/3 11:00~) |
| **Private Score** | **마지막 제출을 끝낸 뒤 파일 1개를 직접 선택.** 안 하면 Public 최고점이 자동 선택되고, 선택 후 추가 제출하면 다시 자동으로 바뀝니다 |
| 코드 제출 요청 | 오면 **2일 이내** 제출. 미제출 시 리더보드 기록 삭제 |

---

## 문의처

- 규칙 해석 공개 질문 → [토크 게시판](https://dacon.io/competitions/official/236736/talkboard)
- 팀 관련 개별 사안 → `aix-server@inha.ac.kr` (리더보드 처리는 `dacon@dacon.io` 참조)
