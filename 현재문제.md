# 지금 막혀 있는 것들

2026-08-06 기준. 해결되면 이 문서에서 지워주세요.

---

## 1. best-of-N 선택 방식이 규정 위반 🚫 최우선

**상태: 확정. 문의 불필요 — 답이 이미 규칙에 있음.** (2026-08-06 23:10 확인)

8/3 11시부터 추가된 조항입니다.

> **사용이 제한되는 사례**
> - Test 데이터의 특징값을 추출하여 예측 결과를 **선택**하거나 보정하는 행위
> - Submission Kit의 실행 결과를 활용하여 최종 mp4 파일을 생성·**선택**·수정하는 행위
> - Submission Kit 내부의 모델·코드·정보를 학습, 추론, 후처리 또는 **앙상블** 과정에 활용하는 행위
> - 그 밖에 Submission Kit을 제출 CSV 생성 외의 목적으로 사용하여 모델 성능에 영향을 주는 행위
>
> AI 모델의 학습과 추론, 후보 영상 생성 및 **최종 영상 선택 과정은 Submission Kit 실행 이전에 모두 완료**되어야 합니다.
>
> 위반 시 **평가·수상 대상에서 제외될 수 있습니다.**

우리는 후보 19벌 각각에 `make_submission_csv.py`를 돌려 Action을 재고, 그 값으로 샘플별 영상을 골랐습니다. **다섯 줄 전부에 걸립니다.**

**따라서**
- 0.164770은 유효하지 않습니다
- 0.26242(정지/생성 샘플별 선택)도 채점기를 썼으므로 무효입니다
- **적법한 최고점은 0.27889** (정지 40%, 자체 계산한 움직임 순위로 선정)

**할 일** — ① 이 방식으로 만든 제출 중단, ② 규칙 4번 "재제출 및 코드 검증 안내" 확인, ③ 자진 신고 여부 팀 결정.

---

## 2. 추론 1시간 제한 초과

**상태: 미해결. 1번 때문에 우선순위가 내려감.**

규칙 원문:
> GPU: RTX PRO 6000 (96GB VRAM) 1대 기준, **추론 시간 제한: 평가 데이터(eval)에 대해 1시간 이내**

평가 요건:
> 제출 코드는 Private Score **완전 복원 필수**

```
216개 전체 생성  x 13벌  =  1,300분
어려운 60개 생성 x  6벌  =    174분
                         ---------
                           1,474분  =  24.6시간   (A40 실측 기준)
```

**왜 이렇게 걸리나** — 216개 자체는 문제가 아닙니다. 영상 하나에 `ddim_steps=25`, guidance 때문에 매 스텝 UNet을 조건부·무조건부 두 번 -> UNet 통과 50회. A40에서 샘플당 27.8초, 216개면 100분. RTX PRO 6000이면 30~50분으로 예산 안입니다. **문제는 19번 한다는 것.**

1번으로 방식 자체가 막혔으므로 이 문제는 자동으로 해소됩니다. 다만 **앞으로 어떤 방식을 쓰든 1시간 예산을 먼저 따져야 합니다.**

"샘플마다 이긴 (시드, guidance)를 216줄 표로 박고 한 번만 돌리기"는 하지 마세요. 그 표를 만드는 데 24.6시간이 들었으므로 계산을 미리 해두고 결과만 재생하는 것이고, 표 자체가 채점기로 만든 것이라 1번에 그대로 걸립니다.

---

## 3. 데이콘 점수 중복 표시

**상태: 미해결. 문의 필요.**

내용이 다른 제출 두 건에 소수점 9자리까지 같은 점수가 표시되는 일이 하루에 두 번:

| 제출 | 표시 점수 |
|---|---|
| hard14 / hard15 | 0.1665129061 (동일) |
| hard16 / hard17 | 0.164770434 (동일) |

로컬 검증으로 두 파일이 확실히 다름을 확인:
- MD5 다름 (`82afabd3…` vs `5328108b…`)
- 크기 다름 (27,772,559 / 27,772,820 바이트)
- 다른 샘플 3개 (`sample_000038`, `sample_000144`, `sample_000166`)
- 자체 측정 Action 다름 (0.098182 / 0.097281)

**실제 점수가 표시된 것보다 나을 가능성이 있습니다.**

---

## 4. GPU 예산 고갈

**상태: 미해결. 팀 차원의 결정 필요.**

```
잔액                                    $6.42
마감까지 15일, 포드 정지 상태 유지비    -$3.60   ($0.01/hr)
                                       ──────
실제 GPU 예산                           $2.82   =  A40 약 6시간
```

그런데 **학습 한 번이 9~14시간, $4~6** 입니다.

**지금 잔액으로는 방향 A 학습을 한 번도 못 돌립니다.** 충전하거나 팀원들이 각자 GPU를 확보해야 모델 개선이 굴러갑니다. 이걸 먼저 정해야 합니다.

참고 비용:
| 작업 | 시간 | 비용 |
|---|---|---|
| 216개 생성 1회 | 100분 | $0.73 |
| 어려운 60개 생성 1회 | 29분 | $0.21 |
| 6000스텝 학습 | 약 8시간 | $3.6 |
| 10000스텝 학습 (실측) | 14시간 | $6.3 |

---

## 5. DINO/Video를 예측할 수 없음

**상태: 구조적 한계. 두 번 시도해 두 번 실패. 사실상 닫힘.**

점수의 60%인데 우리가 아는 건 Action(40%)뿐입니다. DINO/Video는 정답 영상과 비교해야 하므로 **제출해야만 알 수 있습니다.**

- 움직임 특징으로 회귀 → 실패
- 후보들의 합의(다수결)로 대리 → 0.1707, 0.1720 (당시 최고 0.16791보다 나쁨)

합의가 실패한 이유가 시사적입니다. **정지 영상이 합의에서 가장 멉니다(0.2057).** 생성본들이 "가만있어야 할 때 움직이는" 오류를 공유하면 합의가 그 오류를 강화합니다.

즉 **우리는 점수의 40%만 보고 고르고 나머지 60%는 눈 감고 정합니다.** 이게 선택 규칙의 근본 한계입니다.

---

## 6. 4000스텝 체크포인트 존재 여부 미확인

**상태: 포드가 꺼져 있어 확인 못 함. 다음에 켤 때 제일 먼저 볼 것.**

`every_n_train_steps: 2000`, `save_top_k: -1` 설정상 **2000·4000·6000이 모두 남아 있어야 합니다.** 10k 학습도 같은 설정이라 8000·10000까지 있을 수 있습니다.

확인 명령:
```bash
ls -la /workspace/open/baseline/outputs/pretrained_unet_6k/pretrained_unet_6k/checkpoints/
```

있으면 학습 없이 추론만으로 총점 곡선을 확인할 수 있습니다 (어려운 60개 29분, $0.21). 자세한 근거는 README 4번 항목.

---

## 우선순위

1. **규칙 4번 "재제출 및 코드 검증 안내" 전문 확인** — 이미 제출한 것의 처리 절차가 있을 수 있음
2. **자진 신고 여부 팀 결정** (1번) — 코드 검증에서 어차피 드러남
3. **GPU 예산 결정** (4번) — 학습 한 번이 잔액의 절반
4. 담당 A: `action_shift` 학습 / 담당 B: `ddim_steps` 스윕 — 둘 다 적법하고 추론 비용을 안 늘림

**중단** — 채점기를 돌려 후보를 고르는 모든 작업.
