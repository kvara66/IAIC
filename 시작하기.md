# 시작하기

**결론부터 — 이 저장소만으로는 바로 작업을 시작할 수 없습니다.**

여기 있는 건 **무엇을 어떻게 해야 하는지에 대한 지식**입니다. 실제로 돌리려면 대회 배포물과 우리가 만든 산출물이 더 필요합니다. 아래에 무엇을 어디서 구하는지 정리했습니다.

---

## 1. 무엇이 없나

| 필요한 것 | 크기 | 어디서 | 저장소에 있나 |
|---|---|---|---|
| 대회 baseline 소스 (`challenge_kit`) | — | 데이콘 대회 페이지 | ✗ 직접 받으세요 |
| `submission_kit` | — | 데이콘 대회 페이지 | ✗ 직접 받으세요 |
| 학습 데이터 (`data/train`) | 약 10GB | 데이콘 | ✗ 직접 받으세요 |
| 평가 데이터 (`data/eval`) | 216 이미지 + 216 액션 | 데이콘 | ✗ 직접 받으세요 |
| 백본 `backbone.ckpt` | 10.4GB | HuggingFace `Doubiiu/DynamiCrafter_512` | ✗ 직접 받으세요 |
| **우리 학습 체크포인트** `epoch=1-step=6000.ckpt` | 수 GB | **팀 내부 공유** | ✗ **아래 참조** |
| **후보 영상 12종 + CSV** | 630MB | **팀 내부 공유** | ✗ **아래 참조** |
| 학습·추론 설정 | — | — | ✓ `configs/` |
| 조립·측정 스크립트 | 40KB | — | ✓ `tools/` |
| `action_shift` 패치 | — | — | ✓ `patch_shift_orig.py` |

**대회 배포물(위 5개)은 각자 받으시면 됩니다.** 재배포하면 규정 문제가 생길 수 있어 저장소에 안 넣었습니다.

---

## 2. 팀 내부에서 받는 것 — 체크포인트 하나

### 학습 체크포인트

**[epoch=1-step=6000.ckpt (5.36GB) — 구글 드라이브](https://drive.google.com/file/d/1GneuoEBETjiaxtxv93QRRyf9IAbd-36I/view?usp=drive_link)**

**이게 없으면 8시간을 학습해야 합니다** (A40 기준, 약 $3.6). 팀 잔액이 $6.39라 각자 학습하는 건 불가능합니다. 받아서 바로 추론부터 시작하세요.

`configs/eval/*.yaml`의 `act_cond_unet_checkpoint`를 받은 파일 경로로 지정하면 됩니다.

2000·4000 스텝 체크포인트는 **존재하지 않습니다.** 08-06 오전 포드를 잃으면서 사라졌습니다. 자세한 건 [체크포인트.md](체크포인트.md).

### 후보 영상 묶음은 배포하지 않습니다

이전 버전에는 후보 12종(630MB) + 6종(170MB)을 받으라고 적혀 있었는데 **취소되었습니다.**

묶음 안에 각 후보의 `submission_features.csv`가 18개 들어 있는데, 이건 **`make_submission_csv.py`를 돌려 나온 Submission Kit의 실행 결과**입니다. 그리고 이 묶음의 용도 자체가 "기존 후보에 새 후보를 얹어 총점이 얼마나 내려가는지 재는 것" — 즉 **금지된 앙상블 선택**이었습니다.

원본은 팀장이 로컬에 보관 중입니다(데이콘 확인 요청 대비). **배포하지 않습니다.**

### 대신 이걸로 모델 출력을 확인하세요

`생성영상_한눈에.mp4` (1MB) — 216개를 18×12 격자로 담은 2.7초 영상. 모델이 뭘 만드는지 한눈에 봅니다. 팀장에게 요청하세요.

---

## 3. 환경 구축 — 여기서 다섯 번 막혔습니다

전부 `challenge_kit/requirements.txt`에 답이 있으니 **그 파일을 먼저 읽으세요.** 아래는 실제로 막혔던 지점들입니다.

**① pip이 거부당함 (PEP 668)**
```bash
pip install --break-system-packages <패키지>
```
또는 가상환경을 쓰세요.

**② 버전이 너무 최신이면 안 됨**

최신 버전을 깔면 임포트 단계에서 죽습니다. `requirements.txt`에 고정된 버전을 그대로 쓰세요. 저희가 맞춘 조합은 이랬습니다.

```
huggingface-hub==0.25.2
transformers==4.48.3
open_clip_torch==2.22.0
av==12.3.0
omegaconf==2.1.1
einops==0.6.0
pytorch-lightning==1.9.3   # --no-deps 로 설치할 것
```

`pytorch-lightning`을 `--no-deps` 없이 깔면 torch를 제 맘대로 바꿔버립니다.

⚠ 위 목록은 저희 환경 기준이고 `requirements.txt`가 정본입니다. 다르면 그쪽을 따르세요.

**③ `imageio` 누락** — 목록에 없는데 필요합니다.

**④ PYTHONPATH — 이게 제일 헷갈립니다**

```bash
KIT=/workspace/open/baseline/challenge_kit
export PYTHONPATH="$KIT/src:$KIT/libs/dynamicrafter:$KIT/../shared_libs/video_utils"
```

**`shared_libs`가 아니라 `shared_libs/video_utils`입니다.** 중첩 패키지라 한 단계 더 들어가야 합니다. 여기서 한참 헤맸습니다.

**⑤ VRAM** — `batch_size: 4`에서 17.5GB 씁니다. 24GB 카드면 됩니다. `eta > 0`을 쓰면 35GB가 필요하니 주의하세요.

---

## 4. 동작 확인 순서

```bash
cd /workspace/open/baseline/challenge_kit
export PYTHONPATH="$PWD/src:$PWD/libs/dynamicrafter:$PWD/../shared_libs/video_utils"

# 1) 임포트가 되는지
python -c "import lvdm, ldwma; print('ok')"

# 2) 추론 (어려운 60개면 A40 29분, 216개 전체면 100분)
python scripts/inference/generate_baseline_videos.py \
  --config configs/eval/submit_p6k.yaml \
  --prediction-root /workspace/runs/test/videos \
  --challenge-root /workspace/open/data/eval \
  --action-stats-path /workspace/open/data/train/so100_action_statistics.json \
  --seed 0

# 3) 제출 CSV 만들기 (submission_kit 은 절대 수정 금지)
cd /workspace/open/submission_kit
python make_submission_csv.py \
  --prediction-root /workspace/runs/test/videos \
  --challenge-root ../data/eval \
  --action-stats-path ../data/train/so100_action_statistics.json \
  --output-csv /workspace/runs/test/submission_features.csv

# 4) Action 점수 확인 (제출 전에 미리 잴 수 있음)
python /workspace/tools/actioncheck.py /workspace/runs/test/submission_features.csv
```

4번에서 **0.43~0.47 사이**가 나오면 정상입니다. 0.53 근처가 나오면 사전학습 가중치가 안 실린 겁니다 — [체크포인트.md](체크포인트.md) 6번 항목을 보세요.

---

## 5. `tools/` 안에 있는 것

| 스크립트 | 하는 일 |
|---|---|
| `actioncheck.py` | 제출 CSV에서 Action 점수 계산. **제일 자주 씁니다** |
| `mkstatic.py` | 입력 이미지를 16프레임 복사해 정지 영상 216개 생성 |
| `pickN.py` | 후보 N벌을 받아 216개 샘플마다 Action 최소를 골라 조립 |
| `pickhard.py` | 216개짜리 후보 + 60개짜리 후보를 섞어 조립 |
| `hardset.py` | 어려운 60개 샘플을 골라 별도 폴더로 구성 |
| `contrib.py` | 후보별 기여도 측정 (하나씩 빼보며 총점 악화를 잼) |
| `mixfrac.py` | 정지 영상 비율 스윕 |
| `verify_shift.py` | `action_shift` 패치가 0일 때 원본과 동일한지 검증 |
| `patch_shift_orig.py` | 원본 소스에 `action_shift` 추가 (루트에 있음) |

경로가 `/workspace/...`로 하드코딩된 것들이 있으니 각자 환경에 맞게 고치세요.

---

## 6. GPU는 각자 필요합니다

| 작업 | 시간 (A40) | 비용 |
|---|---|---|
| 216개 생성 1회 | 100분 | $0.73 |
| 어려운 60개 생성 1회 | 29분 | $0.21 |
| 6000스텝 학습 | 약 8시간 | $3.6 |

**팀 잔액이 $6.42입니다.** 포드 하나를 나눠 쓰면 순서를 기다려야 하고 병렬이 안 됩니다. **각자 RunPod 계정을 만들거나 다른 GPU를 확보하시는 게 맞습니다.**

RunPod 운영에서 확인한 것 몇 가지.
- 볼륨은 특정 지역에 묶입니다. 포드를 끄면 그 GPU를 남이 가져가고, 재시작하려면 **같은 지역**에서 빈 GPU를 찾아야 합니다. Deploy 화면의 "High availability"는 전 지역 기준이라 재시작 가능 여부와 무관합니다.
- 실제로 08-06 오전에 몬트리올 A40이 소진돼 포드를 잃었습니다. CPU 모드로 켜서 데이터만 빼냈습니다.
- **멈춘 포드도 시간당 $0.01씩 나갑니다.**
- 볼륨 디스크는 증가만 되고 감소는 안 됩니다.

---

## 7. Claude Code로 시작할 때 붙여넣을 요약

```
2026 인하 인공지능 챌린지 baseline 트랙 작업이다.
로봇 첫 프레임 + 16스텝 액션 → 2.7초 미래 영상 생성.
점수 = 0.3×DINO + 0.3×Video + 0.4×Action, 낮을수록 좋음.
현재 팀 최고 0.164770 (205팀 중 2위).

먼저 이 저장소의 README.md, 체크포인트.md, 현재문제.md 를 읽어라.
특히 다음 세 가지를 반드시 지켜라.

1. 추론 시간 제한이 1시간(RTX PRO 6000 기준)이다. 새 방향을 잡기 전에
   그 방향이 이 예산에 들어가는지 먼저 따져라.
2. 가중치 로딩이 strict=False라 모양이 안 맞으면 조용히 건너뛴다.
   로그의 "로드 완료"를 믿지 말고 파라미터를 직접 비교해 확인하라.
3. 지표나 추정으로 무언가를 바꾸려 하면, 적용 전에 기존 데이터로
   사후 검증부터 하라.

내 담당은 [A 또는 B]다. README의 해당 절을 따라 진행하라.
```

---

## 8. 요약 — 오늘 당장 할 수 있는 것

**GPU 없이 지금 되는 것**
- 이 저장소 문서 전부 읽기
- 대회 배포물과 백본 내려받기 (`data/train`이 10GB라 시간이 걸립니다)
- 환경 구축하고 임포트까지 확인
- `configs/train/pretrained_unet_6k.yaml` 읽으며 구조 파악

**체크포인트를 받으면 되는 것**
- 추론 돌려서 0.43~0.47 나오는지 확인
- 담당 A: 4000·8000 체크포인트 추론 (A-1)
- 담당 B: `ddim_steps` 스윕 (B-1)

**후보 묶음까지 받으면 되는 것**
- 0.164770 재현
- 새 후보의 기여도 측정

**체크포인트를 먼저 요청하세요.** 그게 없으면 8시간 학습부터 시작해야 합니다.
