# 6천 체크포인트를 받으신 분께 — 무엇을 바꿔야 하는가

**2026-08-09 작성.** 드라이브로 공유된 `epoch=1-step=6000.ckpt` 를 쓰시려는 분을 위한 문서입니다.

---

## 결론 먼저

**그 체크포인트에서 이어받지 마세요. 설정 네 개를 고쳐서 backbone 부터 다시 돌리는 게 실측으로 더 좋습니다.**

이유는 아래 판정 결과입니다.

---

## 1. 그 체크포인트의 액션 부품은 무작위 상태 그대로입니다

체크포인트를 직접 열어 가중치 분포를 초기화 분포와 비교했습니다.

```
층                         fan_in    기대std      실측std      차이
action_embed.0.weight          6    0.235702    0.235320    0.16%
action_embed.2.weight       1280    0.016137    0.016136    0.01%

null_action_emb   std 0.000000   absmax 0.000000

판정: 초기화 상태 그대로다.
```

**6000스텝을 돌렸는데 액션 임베딩이 한 번도 안 배웠습니다.** 소수점 넷째 자리까지 초기화 분포와 일치합니다.

`null_action_emb` 는 정확히 0 입니다. 단 한 번도 기울기를 받은 적이 없다는 뜻입니다.

### 왜 이렇게 됐나

`ddpm3d.py` 의 `configure_optimizers` 가 **전 파라미터를 한 그룹에 넣고 `lr 1e-5` 하나를 줍니다.**

```
백본에서 온 부품   이미 학습됨 → 1e-5 로 조금씩 다듬으면 맞음
action_embed      백지에서 시작 → 1e-5 로는 사실상 정지
```

**에러가 안 납니다.** 손실도 정상적으로 떨어집니다. 로그만 보면 아무 문제 없습니다.

---

## 2. 이어받기보다 다시 돌리는 게 낫습니다 — 실측

액션 lr 만 고쳐서 backbone 부터 **1에폭**만 돌린 모델이, 6천짜리 **1.14에폭**을 이겼습니다.

| | 6k 체크포인트 | 새로 1에폭 (ck667) |
|---|---|---|
| 출발점 | backbone | backbone (같음) |
| 에폭 | 1.14 (6000 × 유효배치 2) | 1.01 (667 × 유효배치 16) |
| 액션 lr | 1e-5 (전체와 동일) | 1e-5 × **1000** |
| 홀드아웃 recon | 0.06924 | **0.05812** |
| 정지가 더 나은 샘플 | 71% | **20%** |

**실제 리더보드 점수** (전부 적법한 방식, guidance 1.0)

```
6k 순수 출력        0.3497779
6k + 정지 71%       0.2900793
ck667 순수 출력     0.3337385      ← 순수끼리 비교하면 4.6% 개선
정지 영상만 (기준선) 0.3045
```

6k 는 **정지 영상보다 못했습니다.** ck667 에서 처음으로 전부 생성한 쪽이 전부 정지시킨 쪽을 이겼습니다.

**이어받아서 아낄 게 별로 없습니다.** 액션 부품은 어느 쪽이든 백지에서 시작하고, 그게 지배적인 요인이었습니다.

---

## 3. 바꿀 것 — 정확히 네 개

### ① 옵티마이저 그룹 나누기 (필수)

이거 안 하면 나머지 셋이 전부 무의미합니다.

`libs/dynamicrafter/lvdm/models/ddpm3d.py` 의 `configure_optimizers` 에서 파라미터를 이름으로 갈라 두 그룹으로 만듭니다.

```
action_embed / null_action_emb  →  lr × 1000
나머지                           →  lr 그대로
```

패치 스크립트가 이 저장소에 있습니다: [`patch_actlr.py`](patch_actlr.py)
환경변수로 켭니다. 설정 안 하면 원본과 완전히 동일하게 동작합니다.

```bash
export IAIC_ACTION_LR_MULT=1000
```

**배수 100은 실측으로 부족했습니다.**

```
배수 100    100스텝에 0.26% 이동   → 부족
배수 1000   100스텝에 11~40% 이동  → 통과
```

### ② `action_dropout_prob: 0.0` → `0.1`

```yaml
    unet_config:
      params:
        action_dropout_prob: 0.1     # 0.0 이었음
```

0 으로 두면 `null_action_emb` 가 영원히 0 입니다. 그러면 나중에 **액션만 골라서 증폭하는 기법(액션 CFG)을 아예 쓸 수 없습니다.** 6k 가 딱 그 상태입니다.

ck667 은 이걸 0.1 로 학습해서 `null_action_emb` 노름이 5.33 이 됐습니다. 이제 쓸 수 있습니다.

### ③ `accumulate_grad_batches: 2` → `16`

```yaml
lightning:
  trainer:
    accumulate_grad_batches: 16      # 2 였음
```

6k 는 유효 배치가 **2** 입니다. 너무 작아서 기울기가 흔들립니다.

**`batch_size` 는 1 로 두고 `accumulate` 만 올리세요.** A40 48GB 에서 `batch_size: 2` 이상은 OOM 입니다 (실측 VRAM 45.5/46GB).

### ④ `max_steps: 6000` → `667`

```yaml
    max_steps: 667                   # 6000 이었음
```

③으로 유효 배치가 8배 커졌으니 **같은 1에폭이 667스텝**입니다. 6000 을 그대로 두면 8에폭을 도는 셈이 됩니다.

체크포인트 저장 주기도 맞추세요.

```yaml
        every_n_train_steps: 667
```

---

## 4. 이 절차를 꼭 지키세요 — 100스텝 스모크 테스트

**학습을 100스텝만 먼저 돌리고, 체크포인트를 열어서 액션 부품이 실제로 움직였는지 확인하세요.**

```bash
python tools/ckcheck.py <체크포인트경로>
```

가중치 std 를 초기화 분포(`kaiming_uniform`, fan_in 기준)와 비교해 "배웠나 / 안 배웠나"를 30초에 판정합니다.

우리가 8시간을 날린 이유가 이걸 안 해서입니다. **다시 말하지만 에러가 안 납니다.** 열어보기 전엔 모릅니다.

---

## 5. 체크포인트 로딩 함정 — 반드시 읽으세요

`save_only_unet: True` 라서 **체크포인트에는 `model.*` (UNet) 키만 들어 있습니다.**

```
backbone.ckpt   2520개 키   model / first_stage_model / cond_stage_model / embedder / image_proj_model
6k, ck667       1517개 키   model 만
```

그래서 학습 체크포인트를 `pretrained_checkpoint` 에 그냥 넣으면 안 됩니다.

```yaml
# ❌ 이렇게 하면 VAE·CLIP·embedder 가 무작위 초기화됩니다
model:
  pretrained_checkpoint: .../epoch=1-step=667.ckpt
  only_reload_modules: ["model"]
```

`only_reload_modules` 에 다른 이름이 남아 있으면 `ValueError` 로 죽어서 그나마 다행인데, `["model"]` 로 줄이면 **조용히 통과하고 VAE 가 백지가 됩니다.**

**추론할 때는** `act_cond_unet_checkpoint` 를 쓰세요. 이건 backbone 을 먼저 올린 뒤 UNet 만 덮어쓰는 경로라 안전합니다.

```yaml
model_config_file: configs/train/act_full.yaml
act_cond_unet_checkpoint: /경로/epoch=1-step=667.ckpt
```

**이어서 학습할 때는** `pretrained_checkpoint` 는 `backbone.ckpt` 로 두고, UNet 만 따로 덮어쓰는 코드를 추가해야 합니다. 두 파일을 합친 9.8GB 사본을 만들어도 되지만 디스크를 봐야 합니다.

---

## 6. 그리고 조용히 실패하는 함정이 하나 더 있습니다

이건 6k 설정에는 **이미 고쳐져 있습니다.** 새 설정을 만드실 때 되살리지 마세요.

```yaml
model:
  only_reload_modules:
    ["first_stage_model", "cond_stage_model", "embedder", "image_proj_model", "model"]
                                                                            # ↑ 이게 빠져 있었음
  params:
    unet_config:
      params:
        add_act_time_emb: True    # ← False 였음
```

`add_act_time_emb: False` 면 `time_embed_dim` 이 1280 대신 640 으로 잡히고, **모든 ResBlock 의 `emb_layers` 모양이 어긋나 `strict=False` 아래에서 전부 조용히 건너뜁니다.**

**둘 다 고쳐야 합니다.** 하나만 고치면 여전히 안 실립니다. 고친 결과가 `0.51645 → 0.35361` 이었습니다.

---

## 7. 다시 파지 마세요 — 이미 닫은 항목

각각 우리가 몇 시간씩 쓴 것들입니다.

| 항목 | 결과 |
|---|---|
| 0번 프레임을 정답으로 주입 | 이득 **0.38%**. 첫 프레임은 모델이 이미 제일 잘 맞히는 프레임 (오차 0.00417 vs 평균 0.06924) |
| 여백(레터박스) 처리 | 전체 오차의 **0.1%**. 이미 맞게 되어 있음 (여백 -1.0000 vs -0.9953) |
| `action_shift` (명령 한 칸 밀기) | 정답도 생성물도 **이미 lag +1 에서 최고 상관** (0.683 / 0.293). 밀면 어긋남 |
| `max_steps` 미세조정 | 이득 **0.00037**. 곡선이 거의 평평함 |
| fps 불일치 | 학습·평가 **둘 다 6**. 문제 없음 |
| caption 불일치 | **양쪽 다 빈 문자열**. 문제 없음 |
| 추론 배치 크기 | 1 → 4 로 **3%** 만 빨라짐. 게다가 `load_defaults` 가 YAML 값을 무시하고 1로 하드코딩함 (CLI `--batch-size` 로만 먹힘) |

---

## 8. 확인된 추론 설정

```
guidance scale   1.0 이 최적 (올려도 내려도 나빠지는 U자)
ddim_steps       50 > 25
timestep_spacing uniform_trailing
eta              0.0
```

⚠ **1시간 추론 제한에 주의하세요.** A40 에서 216개 × 50스텝 = **100분**입니다.

```
샘플당 27.8초
  고정 비용 17.4초 (62%)   ← VAE 디코드 + CPU 영상 인코딩
  샘플링    12.5초
```

**62% 가 GPU 와 무관한 비용이라 대회 GPU 가 빨라져도 그만큼 안 줄어듭니다.** guidance > 1.0 이나 액션 CFG 는 UNet 호출이 2배라 50스텝에서는 예산을 넘습니다 (25스텝이면 등가).

---

## 9. ck667 을 받으시려면

`act_full_epoch1_step667.ckpt` (5.4GB) 를 드라이브에 올려두겠습니다. 위 네 가지가 이미 반영된 결과물입니다.

같이 봐야 할 설정: [`configs/train/act_full.yaml`](configs/train/)

관련 문서
- [학습_문제.md](학습_문제.md) — 액션 임베딩 미학습 발견의 전체 기록
- [홀드아웃_검증.md](홀드아웃_검증.md) — 킷 없이 설정을 고르는 방법
- [작업전_체크리스트.md](작업전_체크리스트.md) — 작업 시작 전 확인 목록
