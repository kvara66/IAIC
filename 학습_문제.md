# 학습이 거의 안 되어 있었습니다

2026-08-09 발견. **데이터 문제가 아닙니다. 데이터는 멀쩡합니다.** 학습 설정이 문제입니다.

---

## 1. 액션 임베딩이 학습되지 않았습니다

6000스텝 학습한 체크포인트를 열어 `action_embed` 가중치를 봤습니다.

```
action_embed.0.weight   (1280, 6)      std 0.235320   absmax 0.4098
action_embed.2.weight   (1280, 1280)   std 0.016136   absmax 0.0311
```

PyTorch `Linear`의 기본 초기화는 `U(-1/√fan_in, +1/√fan_in)`입니다. 그 분포의 표준편차는

| 층 | 기대 std (초기화) | 실측 std (6000스텝 후) |
|---|---|---|
| `Linear(6, 1280)` | 0.235702 | **0.235320** |
| `Linear(1280, 1280)` | 0.016139 | **0.016136** |

**소수점 4자리까지 일치합니다. 6000스텝을 돌고도 초기화 상태 그대로입니다.**

### 왜 이렇게 됐나

`action_embed`는 **DynamiCrafter 백본에 없는 새 모듈**입니다. 백본 체크포인트에 해당 텐서가 없으니 무작위로 시작합니다. 그런데 `configure_optimizers`가

```python
params = self.get_param_list()          # 전 파라미터를 하나의 리스트로
optimizer = torch.optim.AdamW(params, lr=lr)      # lr = 1e-5 한 값
```

**전 파라미터에 같은 학습률 1e-5를 줍니다.** 사전학습된 UNet에는 적당한 값이지만, 무작위에서 출발하는 새 모듈에는 턱없이 작습니다.

**이건 예전에 시간축 어텐션이 "안 깨어난" 실패와 정확히 같은 원인입니다.** 그때 0으로 초기화한 층이 6000스텝 뒤에도 기존 신호의 0.23%였고, 9시간 40분을 태우고 결론이 안 났습니다. 그때 얻은 교훈이 "새 모듈만 lr을 따로 올려라"였는데, **`action_embed`도 새 모듈이라는 걸 그때 못 알아봤습니다.**

---

## 2. 그리고 1.14 에폭만 돌았습니다

```
학습 에피소드                                    10,544개
batch_size 1 × accumulate_grad_batches 2  =  유효 배치 2
6000스텝 × 2  =  12,000 샘플  =  1.14 에폭
```

**한 바퀴입니다.** 그리고 **대회는 학습을 4일까지 허용하는데 우리는 8시간 썼습니다.**

유효 배치 2에 학습률 1e-5는 아주 천천히, 아주 시끄럽게 배우는 조합입니다.

---

## 3. 이것으로 그동안의 측정이 설명됩니다

| 측정 | 설명 |
|---|---|
| 액션 달성률 43% (정답 0.683 / 생성 0.293) | 액션을 무작위 투영으로 넣고 있으니 절반만 따라감 |
| 생성이 정지 화면보다 나쁨 | 액션 신호가 사실상 잡음이라 엉뚱하게 움직임 |
| guidance 1.0 초과가 나빠짐 | 나쁜 신호를 증폭하니 더 나빠짐 |

**조건화 설계 문제가 아니라 그냥 안 배운 겁니다.**

---

## 4. 뒤집히는 결론들

### "학습 스텝을 늘리는 건 닫혔다" → 다시 열립니다

10000스텝(1.90 에폭)이 6000스텝(1.14 에폭)보다 나빴다는 게 근거였는데, **둘 다 거의 학습이 안 된 구간**이고 유효 배치 2라 기울기가 요동칩니다. 그 차이는 진짜 최적점이 아니라 잡음이었을 가능성이 큽니다.

### "액션을 크로스어텐션으로 보내자" → 순서가 뒤로 갑니다

통로를 바꾸는 것보다 **지금 통로를 배우게 하는 것이 먼저**입니다. 안 배운 상태에서 통로만 바꾸면 똑같이 안 배웁니다.

### `action_shift`는 여전히 폐기

정답·생성 모두 lag +1에서 최고 상관이라 이미 맞아 있습니다. 이건 학습 부족과 별개로 죽은 카드입니다.

---

## 5. 고치는 법

`patch_actlr.py`가 `configure_optimizers`를 고쳐 **액션 모듈만 별도 학습률 그룹**으로 뺍니다.

```bash
export IAIC_ACTION_LR_MULT=100     # 1e-5 → 1e-3 (액션 모듈만)
```

환경변수를 설정하지 않으면 **원본과 완전히 동일하게 동작**합니다.

### 학습 설정 변경안

| 설정 | 현재 | 바꿀 값 | 이유 |
|---|---|---|---|
| `IAIC_ACTION_LR_MULT` | 없음 | **100** | 새 모듈이 깨어나게 |
| `batch_size` | 1 | 4 (VRAM 되는 만큼) | 처리량 |
| `accumulate_grad_batches` | 2 | 4 | 유효 배치 16 |
| `max_steps` | 6000 | 2000 | 2000 × 16 = 32,000 샘플 = **3.0 에폭** |
| `action_dropout_prob` | 0.0 | 0.1 | 액션 CFG를 나중에 쓰려면 필요 |
| `base_learning_rate` | 1e-5 | **그대로** | 한 번에 여러 개 바꾸지 않기 |

### 조기 중단 판정

**100스텝 스모크 테스트를 먼저 돌립니다.** 체크포인트를 열어 `action_embed.2.weight`의 std가 0.016139에서 벗어났는지 봅니다.

- 움직였다 → 패치가 먹었으니 본 학습으로
- 안 움직였다 → **즉시 중단.** $3.6짜리 실험이 $0.5에 판가름납니다

본 학습 중에도 체크포인트마다 홀드아웃으로 재서, 좋아지지 않으면 자릅니다.

---

## 6. 데이터는 문제가 없습니다

혹시 몰라 적어둡니다. **`data/train`을 의심하지 마세요.**

- 에피소드 10,544개, 검증 분할 554개, 전부 정상적으로 읽힙니다
- 액션은 (16, 6) float32, 영상은 640×480
- 정답 영상에서 명령↔화면 상관이 lag +1에서 0.683으로, **데이터 자체에는 따라갈 신호가 충분히 있습니다**

문제는 그 신호를 받아들이는 모듈이 학습되지 않았다는 것뿐입니다.

---

## 7. 비용

A40 실측 처리량이 **시간당 1,500샘플**입니다(batch 1 기준. 배치를 올리면 늘어납니다).

| 목표 | 샘플 | A40 시간 | 비용 |
|---|---|---|---|
| 1.14 에폭 (현재) | 12,000 | 8시간 | $3.6 |
| 스모크 100스텝 | 1,600 | 1시간 | $0.5 |
| **3 에폭** | 32,000 | **21시간** | **$9.3** |
| 5 에폭 | 52,700 | 35시간 | $15.5 |

⚠ 볼륨이 50GB인데 체크포인트 하나가 **5.75GB**입니다. 여유가 19GB라 **3개까지만** 저장됩니다. `every_n_train_steps`를 낮추면 4번째 저장에서 디스크가 차고 학습이 죽습니다.
