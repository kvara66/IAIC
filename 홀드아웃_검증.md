# 홀드아웃 검증 — 킷 없이 설정을 고르는 방법

2026-08-08 구축. **이게 있어야 나머지 작업이 굴러갑니다.**

---

## 왜 필요한가

`submission_kit`은 최종 mp4가 확정된 뒤 제출 CSV를 만들 때만 쓸 수 있습니다. 그러면 **설정을 비교할 때 볼 점수가 없습니다.** 제출은 하루 10회뿐이고, 실험 하나마다 슬롯을 태울 수는 없습니다.

그런데 `data/train`에는 **정답 영상이 있습니다.** 학습에서 빠진 에피소드로 생성해 정답과 직접 비교하면, 킷 없이 숫자가 나옵니다.

> **"홀드아웃은 못 쓴다"는 예전 결론은 이제 유효하지 않습니다.**
> 그건 킷의 Action 지표가 정답 영상에도 1.26을 내서 못 쓴다는 얘기였습니다.
> 우리 지표로 정답과 직접 비교하면 그 문제가 없습니다.

---

## 만드는 법

```bash
source /workspace/env.sh
python tools/mkholdout.py 20 /workspace/holdout
```

데이터셋을 `train=False`로 만들면 **`seed=0`, `val_fraction=0.05`로 학습에서 제외된 554개 에피소드**가 그대로 나옵니다. 거기서 20개를 뽑습니다.

⚠ `__getitem__`을 쓰면 안 됩니다. 시작 프레임을 매번 `rng.randint`로 다시 뽑아서 같은 인덱스라도 다른 구간이 나옵니다. `examples`에 직접 접근해 시작을 고정해야 합니다(에피소드 중앙 사용 — 초반은 정지 구간이 많아 어떤 설정을 써도 비슷해서 비교가 안 됩니다).

출력이 **eval과 같은 형식**이라 추론 스크립트를 한 줄도 안 고치고 돌아갑니다.

```
holdout/images/sample_XXXXXX.png    첫 프레임 (원본 해상도)
holdout/actions/sample_XXXXXX.npy   (16, 6) 정규화 전 액션
holdout/gt/sample_XXXXXX.npy        (16, H, W, 3) 정답 프레임
```

## 재는 법

```bash
python tools/evalholdout.py /workspace/runs/설정A /workspace/runs/설정B
```

| 지표 | 뜻 |
|---|---|
| `recon` | 프레임별 MSE 평균. 정답과 화면이 얼마나 다른가 |
| `motion` | 프레임 간 변화량의 MSE. 움직임이 정답만큼 일어났나 |
| `drift` | 마지막 프레임 오차 / 첫 프레임 오차 |

⚠ **이 숫자와 대회 점수가 얼마나 맞는지는 모릅니다.** 방향을 좁히는 용도이고, 최종 확인은 제출 한 번으로 해야 합니다.

---

## 규정 확인

| 항목 | 판정 |
|---|---|
| submission_kit 관여 | ✅ 전혀 안 씀 |
| eval 데이터 사용 | ✅ 안 씀. train의 검증 분할만 |
| 학습 오염 | ✅ 없음. 이미 학습에서 빠진 에피소드 |
| 샘플별 선택·보정 | ✅ 없음. 설정 하나를 전체에 동일 적용 |
| 제출물 여부 | ✅ 개발 도구, 제출 코드 아님 |

---

## 2026-08-08 측정 결과

**guidance × ddim_steps 격자** (홀드아웃 20개, seed 0)

```
                ddim_steps 25          ddim_steps 50
guidance 0.0           -               0.07263 / 0.01116
guidance 0.5           -               0.06961 / 0.01095
guidance 0.7           -               0.06955 / 0.01075
guidance 1.0    0.07112 / 0.01102      0.06924 / 0.01068   ← 최저
guidance 1.5    0.07415 / 0.01142      0.07166 / 0.01086
guidance 2.5    0.07852 / 0.01202      0.07740 / 0.01131
                        (recon / motion)
```

### 결론 셋

**1. guidance는 1.0이 바닥. U자 곡선입니다.**

0.0에서 나빠지고 1.5 이상에서도 나빠집니다. 0.5·0.7·1.0은 차이가 0.5% 수준이라 사실상 평평합니다.

**0.0이 나쁘다는 게 중요합니다.** 조건화를 끄면 나빠지니 **모델의 조건화는 도움이 되고 있습니다.** 다만 1.0 위로 증폭하면 나빠지는데, guidance가 이미지 조건만 키우고 액션 조건은 안 건드리기 때문으로 보입니다(`dropout_actions=False`가 양쪽 분기 모두).

**2. ddim_steps는 25보다 50이 낫습니다.** recon −2.6%, motion −3.1%. guidance 1.5에서도 −3.4%로 방향이 같습니다. **19벌을 전부 25스텝으로 돌렸던 게 최적이 아니었습니다.**

**3. `batch_size`가 시간 문제를 풉니다.**

```
batch 4     594초 / 20개 (25스텝)
batch 8     440초   1.35배
batch 16    389초   1.53배
```

VRAM은 batch 4나 16이나 17.5GB로 같습니다. 모델 가중치가 대부분이라 배치를 올려도 거의 안 늘어납니다.

216개 환산:
```
                    A40        RTX PRO 6000 (3배 가정)
25스텝 batch16      70분            23분     ✅
50스텝 batch16     140분            47분     ✅  1시간 안
```

### ⚠ 초기 측정에 버그가 있었습니다 (2026-08-09 수정)

`preprocess_video`는 **(채널, 시간, 높이, 너비)** 순서로 반환하는데, 평가기가 첫 축을 시간으로 착각했습니다. 그 결과

| 지표 | 초기 | 수정 후 |
|---|---|---|
| `recon` | ✅ 전체 평균이라 축과 무관, **유효했음** | 그대로 |
| `motion` | ❌ R−G, G−B **채널 간 차이**를 쟀음 | 프레임 간 차이 |
| `drift` | ❌ 파랑÷빨강 채널 오차비 | 프레임15÷프레임0 오차비 |

`recon` 기준 결론(guidance 1.0, 50스텝, 정지 ~50%)은 **전부 그대로 유효**합니다. 다만 "두 지표가 같은 방향이라 신뢰도가 높다"던 근거는 취소합니다.

**수정 후 `drift`는 45~62입니다.** 프레임 0에서 15로 가며 오차가 50배쯤 커집니다. 정상적인 확산 생성 거동입니다.

### 0번 프레임 주입은 닫혔습니다

drift가 1보다 작다고 잘못 읽고 "첫 프레임이 제일 틀렸다"고 판단했는데, **정반대였습니다.**

```
0번 프레임 오차   0.00417
전체 평균         0.06924     ← 16배 큼
```

**모델은 0번 프레임을 제일 잘 맞힙니다.** 정답으로 완벽히 갈아끼워도 recon이 **0.38%**만 좋아지고 motion은 오히려 나빠집니다(−0.27%).

예전에 킷으로 잰 "생성 0번 프레임이 입력과 3.487 어긋남"과는 결론이 다릅니다. **이 항목은 하지 마세요.**

### 정지 출력이 생성보다 낫습니다

`tools/staticfrac.py`로 홀드아웃에서 잰 결과입니다. 이미 만든 영상과 정지 영상을 각각 정답과 비교해두면 어떤 문턱이든 즉시 계산됩니다 — **GPU가 필요 없습니다.**

```
정지비율      recon      motion
   0%       0.06924    0.03212     ← 전부 생성
  40%       0.06241    0.02857
  50%       0.06002    0.02715     ← 최적 (-13.3%)
  70%       0.06294    0.02859
 100%       0.06334    0.02770     ← 전부 정지
```

**전부 정지(0.06334)가 전부 생성(0.06924)보다 좋습니다.** motion에서도 마찬가지입니다.

**즉 지금 모델의 생성은 순손해입니다.** 아무것도 안 움직이는 화면보다 나쁩니다. 옛 채점기 측정(정지 100% = 0.30452 vs 순수 생성 ≈ 0.35)과도 일치합니다.

⚠ 홀드아웃이 20개라 "정확히 50%"는 못 믿습니다. 곡선이 울퉁불퉁하고 40~70% 어디든 될 수 있습니다.

### 문턱은 절대값으로 씁니다

```
holdout  액션 총변화량 중앙값 213.2  (n=20)
eval     액션 총변화량 중앙값 169.2  (n=216)
```

분포가 달라서 비율이 그대로 안 옮겨갑니다. 홀드아웃 최적 문턱 **212.5911**을 eval에 쓰면 정지가 **71%(154/216)**가 됩니다.

그래도 절대값을 씁니다. eval 중앙값을 구해 비율을 맞추면 **eval 전체 분포에서 뽑은 정보로 출력을 정하는 것**이 되어 "Test 데이터에서 추출한 정보를 활용하여 예측 결과를 보정" 조항에 닿습니다. 절대 문턱은 각 샘플이 **자기 입력만** 봅니다.

### 진척 지표로 쓰세요

**모델이 좋아지면 최적 정지 비율이 내려갑니다.** 71% → 50% → 30%로 줄면 생성이 실제로 값을 하기 시작했다는 뜻입니다. 홀드아웃 절대값보다 직관적입니다.

---

## 최종 제출 설정 (2026-08-09)

```
guidance 1.0 · ddim_steps 50 · batch_size 16 · seed 0
```

추론 1회, 216개 전체에 동일 적용, 후처리 없음, 킷은 mp4 확정 후 1회.
