# 체크포인트와 학습 설정

---

## 1. 지금 쓰는 체크포인트

```
/workspace/open/baseline/outputs/pretrained_unet_6k/pretrained_unet_6k/checkpoints/
    epoch=1-step=6000.ckpt        ← 현재 제출에 쓰는 것
```

파일 이름 형식은 `{epoch}-{step}` 입니다 (`filename: "{epoch}-{step}"`).

`save_only_unet: True`, `save_weights_only: True` 라서 UNet 가중치만 들어 있습니다. 옵티마이저 상태가 없어 **이어서 학습(resume)은 안 되고, 추론과 새 학습의 초기값으로만 쓸 수 있습니다.**

---

## 2. 어떤 스텝이 남아 있나 — 실물 확인 결과 (2026-08-06 22:40)

**6000 하나뿐입니다.**

```
/workspace/open/baseline/outputs/pretrained_unet_6k/pretrained_unet_6k/checkpoints/
    epoch=1-step=6000.ckpt        5,754,373,794 바이트 (5.75GB)
```

`pretrained_unet_10k` 폴더는 **존재하지 않습니다.**

설정은 `every_n_train_steps: 2000`, `save_top_k: -1` 이라 2000·4000도 저장됐어야 하고 10k 학습의 8000·10000도 있어야 합니다. 그런데 **08-06 오전 몬트리올 A40 소진으로 포드를 잃으면서 중간 체크포인트와 10k 결과가 함께 사라졌습니다.** 새 포드에는 6000짜리만 넘어왔습니다.

**결과적으로 이렇습니다.**

| 스텝 | 체크포인트 | 측정된 성능 |
|---|---|---|
| 2000 | ✗ 없음 | Action 0.16338 / 총점 0.28259 (기록만 남음) |
| 4000 | ✗ 없음 | 미측정 |
| **6000** | **✓ 있음 (5.75GB)** | Action 0.16194 / 총점 0.28187 |
| 8000 | ✗ 없음 | 미측정 |
| 10000 | ✗ 없음 | Action 0.16596 (기록만 남음) |

**스텝 수 곡선을 공짜로 찍는 건 불가능합니다.** 다른 스텝을 보려면 재학습해야 합니다.

---

## 3. 500단위 체크포인트를 만들려면

```yaml
      every_n_train_steps: 500      # 2000 → 500
```

- 학습 시간은 **똑같습니다** (8시간). 저장을 더 자주 할 뿐입니다.
- **단독으로 돌리면 $3.6이 그냥 나갑니다. 어차피 할 학습(A-3 action_shift)에 얹으면 추가 비용 0입니다.**

### ⚠ 디스크가 부족합니다 — 이거 먼저 계산하세요

```
체크포인트 하나        5.75GB
볼륨 50GB 중 사용      28GB
남는 공간              22GB
        →  추가로 들어가는 개수 = 3개 (12개는 69GB로 택도 없음)
```

**`every_n_train_steps: 500` + `save_top_k: -1` 로 그냥 돌리면 4번째 저장에서 디스크가 차고 학습이 죽습니다.** 8시간짜리 학습을 3시간에 날립니다.

선택지 세 가지입니다.

| 방법 | 얻는 것 | 비용 |
|---|---|---|
| `every_n_train_steps: 2000` 유지 | 2000·4000·6000 (3개) | 0 — 딱 맞음 |
| 볼륨을 80GB로 늘리기 | 500단위 12개 | 30GB 추가, 15일 약 $1.5. **디스크는 늘리기만 되고 줄이기는 안 됨** |
| 저장하면서 바로 빼내기 | 원하는 만큼 | 감시 스크립트 필요. 하나당 5.75GB 전송 |

**추천은 첫 번째입니다.** 2000단위 3개만 해도 곡선에 점 3개가 찍히고, 예상 이득이 0.00037짜리라 500단위까지 갈 값어치가 안 됩니다. 3개를 재보고 곡선이 예상과 다르면 그때 촘촘하게 가세요.

---

## 4. 측정된 스텝별 성능

```
              Action 가중    DINO/Video     총점
2000스텝       0.16338       0.11921       0.28259
4000스텝          ?             ?             ?      ← 미측정 (체크포인트 있음)
6000스텝       0.16194       0.11993       0.28187   ← 현재
8000스텝          ?             ?             ?      ← 미측정 (체크포인트 있음)
10000스텝      0.16596          ?             ?
```

전부 정지 50% 기준입니다.

**읽는 법** — 학습을 늘리면 **Action은 좋아지는데 DINO/Video는 나빠집니다.** 두 성분이 반대로 움직여서 총점 차이가 0.00072밖에 안 납니다. 곡선이 아주 평평합니다.

세 점에 곡선을 맞추면 **총점 최적 약 4,530스텝, 6000 대비 이득 0.00037**입니다. 그런데 **4000의 예상값이 최적점과 0.00005 차이**라 사실상 같습니다.

⚠ **점 세 개로 맞춘 추정이고 DINO/Video가 선형이라고 가정했습니다.** 4000과 8000을 실제로 재보면 확인되거나 깨집니다. 재보기 전엔 확정으로 쓰지 마세요.

---

## 5. 체크포인트로 추론하기

`configs/eval/*.yaml`의 `act_cond_unet_checkpoint`를 바꾸면 됩니다.

```yaml
name: ck4000
group: submit
model_config_file: configs/train/pretrained_unet_6k.yaml
act_cond_unet_checkpoint: /workspace/open/baseline/outputs/pretrained_unet_6k/pretrained_unet_6k/checkpoints/epoch=1-step=4000.ckpt
ddim_kwargs:
  ddim_steps: 25
  unconditional_guidance_scale: 1.0
  timestep_spacing: uniform_trailing
  eta: 0.0
  verbose: False
data:
  target: ldwma.lightning.data_modules.lerobot_so100.SO100DataModule
  params:
    root: /workspace/hard          # 어려운 60개. 전체는 /workspace/open/data/eval
    remote: False
    dataset_paths: auto
    action_stats_path: /workspace/open/data/train/so100_action_statistics.json
    normalize_actions: True
    eval_all_episodes: True
    batch_size: 4
    num_workers: 4
    pin_memory: True
    target_height: 320
    target_width: 512
    pad: True
    camera_key: observation.images.top
    traj_len: 16
    val_fraction: 0.05
    downsample: 1
    use_language: False
```

```bash
export PYTHONPATH="$KIT/src:$KIT/libs/dynamicrafter:$KIT/../shared_libs/video_utils"

python scripts/inference/generate_baseline_videos.py \
  --config configs/eval/ck4000.yaml \
  --prediction-root /workspace/runs/ck4000/videos \
  --challenge-root /workspace/hard \
  --action-stats-path /workspace/open/data/train/so100_action_statistics.json \
  --seed 0
```

**⚠ `model_config_file`은 반드시 학습에 쓴 설정을 가리켜야 합니다.** 모델 구조가 달라지면 가중치가 조용히 안 실립니다(아래).

---

## 6. 조용히 실패하는 함정 — 반드시 읽으세요

가중치 로딩이 `strict=False`라 **모양이 안 맞는 텐서를 예외 없이 건너뜁니다.** 로그에는 "로드 완료"만 찍히고 학습은 정상적으로 손실이 떨어집니다. 그런데 모델은 초기화 상태입니다.

이것 때문에 0.51645에서 헤맸습니다. 두 곳이었습니다.

```yaml
model:
  only_reload_modules:
    ["first_stage_model", "cond_stage_model", "embedder", "image_proj_model", "model"]
                                                                              # ↑ 이게 빠져 있었음
  params:
    unet_config:
      params:
        add_act_time_emb: True    # ← False 였음
```

`add_act_time_emb: False`면 `time_embed_dim`이 1280 대신 640으로 잡히고, 그러면 **모든 ResBlock의 `emb_layers` 모양이 어긋나 전부 건너뜁니다.**

**둘 다 고쳐야 합니다.** 하나만 고치면 여전히 안 실립니다. 고친 결과가 0.51645 → 0.35361입니다.

**새 설정으로 학습을 걸 때는 첫 스텝 전에 UNet 파라미터 몇 개를 백본과 직접 비교해보세요.** 로그를 믿으면 안 됩니다.

---

## 7. 학습 실행

```bash
cd /workspace/open/baseline/challenge_kit
export PYTHONPATH="$PWD/src:$PWD/libs/dynamicrafter:$PWD/../shared_libs/video_utils"

python scripts/train/train.py --config configs/train/pretrained_unet_6k.yaml
```

| | |
|---|---|
| 6000스텝 소요 | 약 8시간 (A40) |
| 10000스텝 실측 | 14시간, $6.3 |
| 대회 허용 | **최대 4일** — 여유 많음 |
| 잔액 | **$6.42** — 여유 없음 |

`max_time: "00:11:00:00"` 이 11시간 상한으로 걸려 있습니다.

---

## 8. 학습 설정에서 안 건드린 값

`configs/train/pretrained_unet_6k.yaml` 전체가 이 저장소에 있습니다. 아래는 손 안 댄 것들입니다.

| 설정 | 현재 | 비고 |
|---|---|---|
| `action_dropout_prob` | `0.0` | **액션 CFG 스위치.** 0보다 크게 줘야 무조건부 분기에서 액션을 뺄 수 있음 |
| `use_ema` | `False` | 확산 모델에서 EMA는 보통 표본 품질을 올림. 미검증 |
| `uncond_prob` | `0.05` | 이미지 조건 드롭 확률 |
| `dropout` | `0.1` | |
| `base_learning_rate` | `1e-5` | **새 모듈을 붙이면 이 값으로는 안 깨어남.** 아래 |
| `use_dynamic_rescale` / `base_scale` | `true` / `0.7` | |
| `rand_cond_frame` | `false` | |
| `accumulate_grad_batches` | `2` | 유효 배치 = 1 × 2 |
| `precision` | `16` | |

**`base_learning_rate: 1e-5` 주의** — 0으로 초기화한 새 층을 붙였더니 6000스텝 뒤에도 기존 신호의 0.23%였습니다. 9시간 40분을 태우고 결론이 안 났습니다. 새 모듈을 붙이신다면 ① 0이 아닌 값으로 초기화하고 ② 그 모듈만 `lr: 1e-3`으로 따로 주고 ③ 2000스텝에서 중단 판정을 하세요.
