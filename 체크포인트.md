# 체크포인트와 학습 설정

> **2026-08-09 갱신.** 이 문서의 이전 버전은 6000스텝 체크포인트를 "현재 제출에 쓰는 것"으로 적고 있었고, 스텝별 성능 표를 근거로 제시했습니다. **둘 다 지금은 유효하지 않습니다.**
>
> - 6000스텝 체크포인트는 **액션 임베딩이 초기화 상태 그대로**임이 확인됐습니다 → [6천에서_바꿀것.md](6천에서_바꿀것.md)
> - 아래 4절의 스텝별 성능 수치는 **submission_kit 으로 잰 값**이라 규정상 의사결정에 쓸 수 없습니다. 기록으로만 남깁니다.

---

## 1. 지금 쓰는 체크포인트

```
/workspace/open/baseline/outputs/act_full/act_full/checkpoints/
    epoch=1-step=667.ckpt          ← 현재 최신 (5.4GB)
```

`IAIC_ACTION_LR_MULT=1000` 과 `action_dropout_prob: 0.1` 로 backbone 부터 1에폭 학습한 것입니다.
설정: `configs/train/act_full.yaml`

### 이전 것

```
/workspace/open/baseline/outputs/pretrained_unet_6k/pretrained_unet_6k/checkpoints/
    epoch=1-step=6000.ckpt         5.75GB  ← 액션 임베딩 미학습. 새로 쓰지 마세요
```

**두 체크포인트 비교**

| | 6k | ck667 |
|---|---|---|
| 출발점 | backbone | backbone (같음) |
| 에폭 | 1.14 (6000 × 유효배치 2) | 1.01 (667 × 유효배치 16) |
| 액션 lr | 전체와 동일 (1e-5) | 1e-5 × **1000** |
| `action_dropout_prob` | 0.0 | **0.1** |
| `action_embed` 학습 여부 | **초기화 상태 그대로** | 학습됨 |
| `null_action_emb` | 정확히 0 | 노름 5.33 |
| 홀드아웃 recon | 0.06924 | **0.05812** |
| 정지가 더 나은 샘플 | 71% | **20%** |
| 리더보드 (순수 출력) | 0.3497779 | 0.3337385 |

---

## 2. 체크포인트에 무엇이 들어 있나

`save_only_unet: True`, `save_weights_only: True` 라서 **UNet 가중치만** 들어 있습니다.

```
backbone.ckpt   2520개 키   model / first_stage_model / cond_stage_model / embedder / image_proj_model
6k, ck667       1517개 키   model 만
```

옵티마이저 상태가 없어 **lightning 의 resume 은 안 되고**, 추론과 새 학습의 초기값으로만 쓸 수 있습니다.

### ⚠ 이것 때문에 조용히 망가질 수 있습니다

```yaml
# ❌ VAE·CLIP·embedder 가 무작위 초기화됩니다
model:
  pretrained_checkpoint: .../epoch=1-step=667.ckpt
  only_reload_modules: ["model"]
```

`only_reload_modules` 에 `first_stage_model` 등이 남아 있으면 `ValueError` 로 죽어서 그나마 알 수 있는데, `["model"]` 로 줄이면 **조용히 통과합니다.**

**추론은** `act_cond_unet_checkpoint` 를 쓰세요. backbone 을 먼저 올린 뒤 UNet 만 덮어쓰는 안전한 경로입니다.
**이어서 학습은** `pretrained_checkpoint` 를 `backbone.ckpt` 로 두고 UNet 만 따로 덮어쓰는 코드를 추가해야 합니다.

---

## 3. 조용히 실패하는 함정 — 반드시 읽으세요

가중치 로딩이 `strict=False` 라 **모양이 안 맞는 텐서를 예외 없이 건너뜁니다.** 로그에는 "로드 완료"만 찍히고 손실도 정상적으로 떨어집니다. 그런데 모델은 초기화 상태입니다.

이것 때문에 0.51645 에서 헤맸습니다. 두 곳이었습니다.

```yaml
model:
  only_reload_modules:
    ["first_stage_model", "cond_stage_model", "embedder", "image_proj_model", "model"]
                                                                            # ↑ 이게 빠져 있었음
  params:
    unet_config:
      params:
        add_act_time_emb: True    # ← False 였음
```

`add_act_time_emb: False` 면 `time_embed_dim` 이 1280 대신 640 으로 잡히고, **모든 ResBlock 의 `emb_layers` 모양이 어긋나 전부 건너뜁니다.**

**둘 다 고쳐야 합니다.** 하나만 고치면 여전히 안 실립니다. 고친 결과가 `0.51645 → 0.35361` 입니다.

그리고 **같은 방식으로 두 번째 함정이 있었습니다.** 위를 고쳐도 `action_embed` 는 여전히 안 배웁니다. 그건 모양 문제가 아니라 학습률 문제입니다 → [학습_문제.md](학습_문제.md)

**새 설정으로 학습을 걸 때는 100스텝 뒤에 `tools/ckcheck.py` 로 판정하세요. 로그를 믿으면 안 됩니다.**

---

## 4. (기록) 스텝별 성능 — 의사결정에 쓰지 마세요

> ⚠ **아래 수치는 전부 submission_kit 으로 측정한 값입니다.** 규정 7조상 킷에서 나온 정보는 학습·추론·선택 어디에도 쓸 수 없습니다. 삭제하지 않고 남기는 이유는 "이런 걸 쟀었다"는 기록 때문입니다. **새 판단의 근거로 인용하지 마세요.**

```
              Action 가중    DINO/Video     총점
2000스텝       0.16338       0.11921       0.28259
6000스텝       0.16194       0.11993       0.28187
10000스텝      0.16596          ?             ?
```

전부 정지 50% 기준입니다. 08-06 포드 소실로 2000·10000 체크포인트 실물은 없습니다.

당시 결론은 "총점 최적 약 4,530스텝, 6000 대비 이득 0.00037" 이었고, **곡선이 거의 평평해서 스텝 수 튜닝은 값어치가 없다**는 것이었습니다. 이 결론 자체는 지금도 닫힌 항목으로 둡니다.

**대신 지금 유효한 판정은 홀드아웃으로 합니다** → [홀드아웃_검증.md](홀드아웃_검증.md)

---

## 5. 체크포인트로 추론하기

```yaml
name: ck667
group: submit
model_config_file: configs/train/act_full.yaml        # 반드시 학습에 쓴 설정
act_cond_unet_checkpoint: /workspace/open/baseline/outputs/act_full/act_full/checkpoints/epoch=1-step=667.ckpt
ddim_kwargs:
  ddim_steps: 50
  unconditional_guidance_scale: 1.0
  timestep_spacing: uniform_trailing
  eta: 0.0
  verbose: False
data:
  target: ldwma.lightning.data_modules.lerobot_so100.SO100DataModule
  params:
    root: /workspace/open/data/eval
    remote: False
    dataset_paths: auto
    action_stats_path: /workspace/open/data/train/so100_action_statistics.json
    normalize_actions: True
    eval_all_episodes: True
    batch_size: 4
    num_workers: 4
    pin_memory: True
    target_height: 320
    target_width: 512
    pad: True
    camera_key: observation.images.top
    traj_len: 16
    val_fraction: 0.05
    downsample: 1
    use_language: False
```

```bash
export PYTHONPATH="$KIT/src:$KIT/libs/dynamicrafter:$KIT/../shared_libs/video_utils"

python scripts/inference/generate_baseline_videos.py \
  --config configs/eval/ck667.yaml \
  --prediction-root /workspace/runs/ck667/videos \
  --challenge-root /workspace/open/data/eval \
  --action-stats-path /workspace/open/data/train/so100_action_statistics.json \
  --seed 0
```

⚠ **`model_config_file` 은 반드시 학습에 쓴 설정을 가리켜야 합니다.** 구조가 달라지면 가중치가 조용히 안 실립니다.

⚠ **`batch_size` 는 YAML 값이 무시됩니다.** `load_defaults` 가 1로 하드코딩합니다. CLI `--batch-size` 로만 먹힙니다. (그리고 실측 이득은 3% 뿐입니다.)

### 1시간 추론 제한

A40 에서 216개 × 50스텝 = **100분**입니다.

```
샘플당 27.8초
  고정 비용 17.4초 (62%)   ← VAE 디코드 + CPU 영상 인코딩
  샘플링    12.5초
```

**62%가 GPU 와 무관한 비용이라 대회 GPU 가 빨라져도 그만큼 안 줄어듭니다.** guidance > 1.0 이나 액션 CFG 는 UNet 호출이 2배라 50스텝에서는 예산을 넘습니다 (25스텝이면 등가).

---

## 6. 학습 실행

```bash
cd /workspace/open/baseline/challenge_kit
source /workspace/env.sh
export IAIC_ACTION_LR_MULT=1000          # 이게 없으면 액션 임베딩이 안 배웁니다

bash scripts/train.sh --config configs/train/act_full.yaml --script scripts/train_diffusion.py
```

| | |
|---|---|
| 1에폭 (667스텝, 유효배치 16) | 약 8시간 (A40) |
| 대회 허용 | **최대 4일** — 여유 많음 |
| VRAM | 45.5 / 46GB. `batch_size: 2` 이상은 OOM |
| 체크포인트 하나 | 5.4GB. 볼륨 50GB 이라 3개까지만 |

---

## 7. 학습 설정에서 아직 안 건드린 값

| 설정 | 현재 | 비고 |
|---|---|---|
| `use_ema` | `False` | 확산 모델에서 EMA 는 보통 표본 품질을 올림. 미검증 |
| `uncond_prob` | `0.05` | 이미지 조건 드롭 확률 |
| `dropout` | `0.1` | |
| `base_learning_rate` | `1e-5` | 액션 모듈은 `IAIC_ACTION_LR_MULT` 로 별도 |
| `use_dynamic_rescale` / `base_scale` | `true` / `0.7` | |
| `rand_cond_frame` | `false` | |
| `perframe_ae` | `True` | |
| LR 스케줄러 | **없음** | `linear_warmup_steps: 200` 뿐. 장시간 학습 시 코사인 decay 검토 필요 |
| `precision` | `16` | |

**바뀐 값** (6k → act_full)

| 설정 | 6k | act_full |
|---|---|---|
| `action_dropout_prob` | `0.0` | **`0.1`** |
| `accumulate_grad_batches` | `2` | **`16`** |
| `max_steps` | `6000` | `2000` (667에서 저장) |
| `every_n_train_steps` | `2000` | `667` |
| 액션 lr 배수 | 없음 | **1000** (`patch_actlr.py`) |
